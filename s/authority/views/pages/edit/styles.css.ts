
import {css} from "lit"
export default css`

:host {
	display: flex;
	flex-direction: column;
	gap: 1em;
}

.delete-heading {
	color: var(--angry);
}

.seedtext {
	width: 100%;
	align-items: stretch;
	color: var(--seed);
}

`

